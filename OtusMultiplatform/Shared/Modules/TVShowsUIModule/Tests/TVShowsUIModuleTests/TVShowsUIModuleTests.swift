import XCTest
@testable import TVShowsUIModule

final class TVShowsUIModuleTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TVShowsUIModule().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
